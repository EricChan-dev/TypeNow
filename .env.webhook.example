# GitHub Webhook 的 Secret。
#
# 用法：
#   1. 生成密钥：  openssl rand -hex 32
#   2. 填到 GitHub → 仓库 Settings → Webhooks → Secret（Content type 选 application/json）
#   3. 复制本文件为 .env.webhook 并填入同一个值，然后 chmod 600 .env.webhook
#   4. pm2 restart webhook
#
# 说明：.env.webhook 会被 .gitignore 的 `.env*` 规则忽略，不会被提交。
# webhook-server.js 未配置该值时会**拒绝所有请求**（fail-closed），
# 因为该入口一旦敞开，任何人都能反复触发生产构建。
WEBHOOK_SECRET=
