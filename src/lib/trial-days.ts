/**
 * 体验会员天数。
 *
 * 单独放一个「无任何服务端依赖」的模块，是因为这个数字既要被服务端用
 * （lib/trial.ts 的授予逻辑），也要被客户端组件直接显示（试学墙的按钮文案、
 * 领取弹窗的标题）。客户端若从 lib/trial.ts 取，会把 db / drizzle 一起拖进
 * 浏览器 bundle，所以两边统一从这里取，保证文案与实发天数永远一致。
 */
export const TRIAL_DAYS = 5
